import { observer } from 'mobx-react-lite';

export const RequestHistory = observer(() => {
    return (
        <>
            <h1 className='mt-20 text-[32px] font-semibold'>В разработке</h1>
        </>
    );
});